# Adopt the real GCD (bakobo/schema) for the seat credential and the device grant, and pay the record churn
kind: todo
created: 2026-09-11T17:24Z

- 2026-09-11T17:24Z Daniel ruled 2026-09-11: adopt the real GCD for BOTH the seat credential and the device grant, and pay the record churn. Supersedes SEAT_SCHEMA (acme/build.py:127) and settles tick 3un3 in the affirmative direction: authority is conferred by a revocable credential, never by the KERI delegation relationship, which is permanent and confers nothing.

THREE FINDINGS FROM READING bakobo/schema, one of which corrects what I told him.

1. presentsAs does NOT gate. I claimed facet.presentsAs was beat 15's slot predicate. Wrong. schema/gcd/index.md:78 says "The facet is descriptive; only constraints gates the authorization decision", and schema/this.i (the facet node) says facet "stays PERMISSIVE ... because ONLY constraints is the fail-closed gate -- the facet is descriptive accountability a verifier MAY ignore for the authorization decision." So presentsAs is a display/transparency fact for the seat screen, not a predicate. The slot predicate keys on the GCD's issuer (i) and issuee (a.i) instead, which is stronger anyway because those are the credential's identity rather than its metadata.

   WORTH AN ISSUE against bakobo/schema: gcd.schema.json:113 says "Presenting-as without this granted capability is impersonation", which reads as a normative gate and contradicts index.md:78 and the facet intent node. Reconcilable, but a verifier reading only the field description would build the wrong thing -- which is exactly what I did.

2. VERSION: use gcd-2.0.1, $id EAqOeo_YMHDEMZ-dIJTYd72nsoUS-C1RdXtOdfAj7ZxR. Not a preference -- forced by the substrate. Current GCD is 3.1.x ($id EDqAod5ZiCNfQziVHjOALNNRabw2iwpAYqqOsEXUcxm5), which requires rd and the ACDC v2 envelope [v,t,d,u,i,rd,s,a,e,r] with v2 most-compact SAIDs. keripy's credential minting path (vc/proving.py:19-50) builds only the v1 envelope; the acm ilk appears solely in core/serdering.py. this.i @exy3u4t7 already recorded that keripy's v2 defaults raise. index.md calls 2.0.1 "the same semantic content on the v1 envelope", so this is a real published GCD with a real SAID, not a lookalike.

3. CONSTRAINTS SCOPE, which is what the fold must actually evaluate. GCD rule 1 (index.md, "All fields inside constraints"): "Nothing outside constraints constrains, and an unrecognized key INSIDE constraints is fail-closed -- a verifier that does not recognize it MUST assume that constraint is unmet and MUST deny." That is utina's own posture (fold/slots.py:33-37) arriving from the other direction, so the rule and the engine agree.
   utina's fold can honestly evaluate: acts (the effect x state-kind grid -- pure string parsing, no KERI, no clock) and domains (set membership).
   It CANNOT evaluate: goals, jurisdictions, physGeos, virtGeos, icals, monetaryLimit, protos, proofs, humanReview -- each needs context no committed byte carries. validFrom/validUntil need a clock, and a wall clock is the ambient input axiom 2 forbids; judging them against the citing event's own committed dt is arguable and is NOT being taken without a ruling.
   So: Acme's two GCDs carry acts and nothing else, and the slot predicate denies on any other constraint key present. That makes the demo show the engine refusing what it cannot check, which is a better beat than silent acceptance.
- 2026-09-11T18:32Z 2026-09-11: F-E7BQ filed as bakobo/schema#3 (Daniel authorized filing on his own repos for this finding).
- 2026-09-11T19:12Z 2026-09-11 PROGRESS: four commits landed — 537be23 (intent), e99be63 (fold/acts.py, the act grid), ea7b6dd (seam rules keyword + both GCD pins vendored and recomputed), 0609d62 (Acme's seat credential is a GCD; Constructor.seat -> Constructor.confer; SEAT_SCHEMA and schemas/acme-seat.json retired). 1021 pass, 16 skip, 100% branch, ruff+mypy clean, demo-1 verdicts unchanged, no pinned digest moved. Second ambiguity filed as bakobo/schema#4 (absent acts: unconstrained vs empty surface). REMAINING: Nina's device delegated from the seat, the seat's GCD grant to it (confer(device, issuer=seat, presents_as=seat)), and the slot predicate's second arm — an endorsement fills a slot when a standing GCD issued by the slotted endorser names the acting AID as issuee, found by searching the record, with acts covering ('create','commitment') and any unimplemented constraint key denying.
- 2026-09-11T19:37Z 2026-09-11 DONE for the GCD adoption proper: 6f50f9b (slot predicate's second arm — a delegate fills its granter's slot via a standing GCD found by SEARCH, with constraints gated twice), 0f7b1b3 (the device, the grant, the Q2 forecast; oracle rows 13 and 15 green), plus an intent commit for the registry-per-controller finding. 1054 pass, 14 skip, 100% branch, all four gates green, demo-1 verdicts unchanged.

REAL FINDING worth carrying forward: the keripy backend caught a design error the facade accepted silently. A TEL takes its authorization from a seal in the CONTROLLER's key log, so the seat's grant into Acme's registry stayed in escrow and read as unissued. Fixed by giving the seat its own registry — which is the better governance shape anyway, because revocation authority follows the registry's controller. Generalizes: any conferring party needs a registry of its own. Watch for this again when Quinn and the second amendment land.

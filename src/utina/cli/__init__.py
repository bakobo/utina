"""The command line: a read surface over the fold, and the constructor's verb.

Nothing here computes governance. Every verdict on a screen came out of
``utina.fold.evaluate`` and every committed byte out of ``utina.enact``; this package
resolves what a person typed, asks one question, and renders the answer with the ground
the answer carries.

``docs/intent-cli.md`` holds the decisions. ``docs/render-candidates.md`` holds the three
renderings of the D3 screen the default was chosen from.
"""

from utina.cli.app import Console, main, run

__all__ = ["Console", "main", "run"]

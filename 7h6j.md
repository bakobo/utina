# utina.enact offers no way to resume a committed record; utina.cli reaches into Constructor privates
kind: todo
created: 2026-08-12T00:26Z

- 2026-08-12T00:26Z The CLI's enact verb has to continue Acme's committed log: Constructor._emit takes its coordinate from len(self._emitted), _dispose checks self._saids, and every verb checks self._founded. utina.cli.world.constructor_over sets all three from the record, marked ~7h6j. The fix is a public Constructor.resume(events) on utina.enact; the CLI then drops the adapter. tests/test_cli.py::test_the_constructor_adapter_continues_the_committed_record pins the invariants meanwhile.
